# Copy to compose.env (same folder) before: docker compose --env-file deploy/compose.env up -d

POSTGRES_USER=boat
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=boat
POSTGRES_PORT=5432
API_PORT=3001
